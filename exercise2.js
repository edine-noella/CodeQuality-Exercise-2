function greetPerson(name) {
    // if (name) {
    //   return "Hello, " + name + "!";
    // } else {
    //   return "Hello!";
    // }

    return (name) ? `Hello, ${name} !` : "Hello!";
  }

    console.log(greet("")); 

